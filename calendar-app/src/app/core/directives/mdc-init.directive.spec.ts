import { MdcInitDirective } from './mdc-init.directive';

describe('MdcInitDirective', () => {
  it('should create an instance', () => {
    const directive = new MdcInitDirective();
    expect(directive).toBeTruthy();
  });
});
